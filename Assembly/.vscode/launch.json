{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/.macOS/${workspaceFolderBasename}.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}